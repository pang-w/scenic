package com.itmaoo.scenic.robot.nuke.cilei;
/**
 * 
 * 1）表示时间，处所：从 自 自从 于 打 到 往 在 当 朝 向 顺着 沿着 随着
（2）表示方式：按 照 按照 依 依照 本着 经过 通过 根据 以 凭
（3）表示目的：为 为了 为着
（4）表示原因：因 由于 因为只有
（5）表示对象，范围：对 对于 把 向 跟 与 同 给 关于
（6）表示排除：除 除了 除去 除非
（7）表示被动：被 叫 让 给 使才
（8）表示比较：比 和 与 同
 * @author mario
 *
 */
public enum JieCi {
	ShiJian,FanShi,MuDi,YuanYin,DuiXiang,PaiChu,BeiDong,BiJiao
}
